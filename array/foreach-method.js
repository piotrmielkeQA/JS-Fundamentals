const names = ["Alicja", "Tomasz", "Agnieszka", "Beata", "Rafał", "Klaudiusz"]
const numbers = [24, 63, 41, 42, 68, 85]
const emptyTable = [];



// Map method return empty table []
console.log(
    emptyTable.map(item => {
        return item;
    })
)

// forEach method return undefined
console.log(
    emptyTable.forEach(item => {
        return item;
    })
)
